import Foundation

/// Builds a personal profile of how the user's body responds to different apps.
/// Currently a minimal stub — will be expanded with ML-based personalization.
class PersonalProfileBuilder {

    func response(for appName: String) -> AppBodyResponse? {
        nil
    }
}
